# NanoPi R2S 图形化刷机工具

![r2sflasher](images/luci-app-r2sflasher.png)

支持上传常用压缩格式的固件包图形化刷机。

从[这里下载](../FriendlyWRT)的 zip 包中会含有 ipk 安装文件，可以用 luci 界面的文件传输安装，或在 ssh 中执行安装命令 `opkg install luci-app-r2sflasher*.ipk --force-depends`
